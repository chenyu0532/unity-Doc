面试中经常被问到：
	如果有一个属性是public UnitConfig unitConfig;
	如果场上有大量单位，为什么不能一个单位就分配一个unitConfig？

其实考的是值类型与引用类型的差异

区别：值类型就是一个随用随取的东西，用完就"扔"，比如Vector3、Quaternion、Color 全是struct，全是值类型，直接存的是数据

 	  而引用类型真身在托管堆上，定义的这个变量只是一个地址，堆得首先找到空闲空间，然后分配，最后GC，适合那些长期存储的对象


回到这个例子，每创建一个单位就要在堆上分配内存，当上千个单位时，GC必定暴了。

所以需要所有同类型单位共享同一个unitConfig实例

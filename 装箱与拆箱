装箱：将值类型(int float struct等)转换到引用类型(class、object、数组、接口、委托等)的操作。
      因为是在堆内存操作，所以会引发GC垃圾回收
拆箱：相反操作，，但不释放堆上的对象

首先要明白的是：值类型是在栈上的，而引用类型是放在堆上的

unity中的ArrayList HashTable ---> List
      比如 arrayList.Add(new 结构体) //触发装箱

      再比如 事件系统中
            public event Action<object> OnPlayerStateChanged;
            // 触发时
            PlayerState state = new PlayerState();  // struct
            OnPlayerStateChanged?.Invoke(state);  // 隐式装箱！

tbd



